---
label: post-item
hide_body: false
fields:
- name: thumbnail
  type: file
  config:
    maxSize: 64
  label: Thumbnail
- name: title
  type: text
  config:
    required: true
  label: Title
- name: content
  type: textarea
  default: ''
  config:
    required: true
    wysiwyg: true
    schema:
      format: html-blocks
  label: Content
- name: above_content
  type: blocks
  label: Above Content
  template_types:
  - navigation-header-w-button
  config:
    min: 
    max: 
- name: below_content
  type: blocks
  label: Below Content
  template_types:
  - simple-footer
  config:
    min: 
    max: 
pages:
- _posts/2020-10-08-krispy-kreme-store.md
